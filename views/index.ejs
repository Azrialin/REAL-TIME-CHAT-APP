<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Web Chat App</title>
    <!-- load socket.io cdn -->
    <script
      src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/4.7.2/socket.io.js"
      integrity="sha512-zoJXRvW2gC8Z0Xo3lBbao5+AS3g6YWr5ztKqaicua11xHo+AvE1b0lT9ODgrHTmNUxeCw0Ry4BGRYZfXu70weg=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    ></script>
    <script defer src="script.js"></script>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
      crossorigin="anonymous"
    ></script>
    <style>
      body {
        background-color: #5865f2;
        color: white;
        display: flex;
        flex-direction: column; /* 垂直堆疊 */
        align-items: center; /* 水平居中 */
        justify-content: center; /* 垂直居中 */
        height: 100vh; /* 使用100%的視窗高度 */
      }

      a {
        color: white;
      }
      
      .logout {
        position: fixed;
        top: 0;
        right: 0;
        margin: 10px;
      }
    </style>
  </head>
  <body>
    <form action="/logout" method="post" class="logout">
      <button type="submit" class="btn btn-light">Log out</button>
    </form>

    <div id="room-container">
      <% Object.keys(rooms).forEach(room => { %>
      <div class="mb-2">
        <!-- mb-2 是Bootstrap的工具類，表示bottom margin是0.5rem -->
        <span class="me-2">Room : <%= room %></span>
        <a href="/<%= room %>" class="btn btn-dark btn-sm">Join</a>
      </div>
      <% }) %>
    </div>

    <form action="/room" method="POST">
      <div class="input-group">
        <!-- 使用input-group組合input和button -->
        <input name="room" type="text" class="form-control" placeholder="Enter room name" required/>
        <div class="input-group-append">
          <button type="submit" class="btn btn-success">New Room</button>
        </div>
      </div>
    </form>
  </body>
</html>
